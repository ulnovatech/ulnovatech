# Copy to keystore.properties (same folder) and fill in your release keystore details.
# Never commit keystore.properties or the .keystore file itself.

storeFile=../keystore/ulnova-admin-release.keystore
storePassword=your-keystore-password
keyAlias=ulnova-admin
keyPassword=your-key-password
