# Full stack: ulnovatech (nginx + php-fpm + mysql) + Discovery Intelligence
#   docker compose -f infra/docker-compose.full.yml up -d --build
# Or: npm run docker:full

include:
  - docker-compose.yml
  - docker-compose.discovery.yml
